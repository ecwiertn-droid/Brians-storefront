export default function DemoBanner() {
  return (
    <div
      style={{
        background: "#fff3e0",
        borderBottom: "1px solid #e8813d",
        color: "#5c3a1e",
        fontSize: "0.85rem",
        textAlign: "center",
        padding: "6px 10px",
      }}
    >
      Preview mode — showing sample content. Connect Supabase to replace this
      with real hours, menu, and trainer data.
    </div>
  );
}
